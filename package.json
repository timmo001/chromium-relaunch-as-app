{
  "name": "chromium-relaunch-as-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.2",
  "scripts": {
    "build": "bun run build:extension && bun run build:native",
    "build:extension": "bun build src/background.ts src/popup.ts --target=browser --format=iife --outdir=extension/dist",
    "build:native": "mkdir -p dist && bun build src/native/relaunch-host.ts --compile --outfile dist/relaunch-current-page-host && bun build src/native/browser-urls-host.ts --compile --outfile dist/browser-urls-host && bun build src/native/install-native-host.ts --compile --outfile dist/install-native-host",
    "check": "bun run lint && bun run typecheck && bun run test && bun run format:check && bun run build",
    "format": "prettier --write \"{src,tests}/**/*.ts\" oxlint.config.ts package.json tsconfig.json",
    "format:check": "prettier --check \"{src,tests}/**/*.ts\" oxlint.config.ts package.json tsconfig.json",
    "lint": "oxlint",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "prepare": "effect-tsgo patch"
  },
  "dependencies": {
    "@effect/platform-bun": "4.0.0-rc.117",
    "effect": "4.0.0-rc.117"
  },
  "devDependencies": {
    "@effect/tsgo": "0.45.0",
    "@effect/vitest": "4.0.0-rc.112",
    "@oxlint/plugins": "1.83.0",
    "@timmo001/oxlint-rules": "0.3.5",
    "@types/bun": "1.4.2",
    "@types/chrome": "0.3.0",
    "oxlint": "1.83.0",
    "prettier": "3.9.8",
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  }
}
